'use client';\n\nimport { signIn, signOut, useSession } from 'next-auth/react';\nimport { Button } from '@/app/components/Button';\nimport { Bot } from 'lucide-react';\n\nexport function Navbar() {\n  const { data: session } = useSession();\n\n  return (\n    <nav className="fixed top-0 left-0 right-0 z-50 bg-[#06162c]/80 backdrop-blur-lg border-b border-white/10\">\n      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8\">\n        <div className="flex justify-between items-center h-16\">\n          <div className="flex items-center gap-2\">\n            <Bot className="w-8 h-8 text-[#00c2ff]" />\n            <span className="text-xl font-bold text-white">BusinessReviewBot</span>\n          </div>\n\n          <div className="flex items-center gap-4\">\n            {session ? (\n              <>\n                <span className="text-white/80 text-sm hidden sm:inline\">\n                  {session.user?.email}\n                </span>\n                <Button onClick={() => signOut()} variant="secondary">\n                  Sign Out\n                </Button>\n              </>\n            ) : (\n              <>\n                <Button onClick={() => signIn('google')} variant="secondary">\n                  Sign In\n                </Button>\n                <Button onClick={() => signIn('google')} variant="primary\">\n                  Get Started\n                </Button>\n              </>\n            )}\n          </div>\n        </div>\n      </div>\n    </nav>\n  );\n}